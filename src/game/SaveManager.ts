export interface Profile{nickname:string;level:number;xp:number;totalKills:number;gamesPlayed:number;victories:number;highestWave:number;totalGoldEarned:number}
const KEY='green-valley-profile';export class SaveManager{static load():Profile|null{try{const v=JSON.parse(localStorage.getItem(KEY)||'null');return v&&typeof v.nickname==='string'?v:null}catch{return null}}static save(v:Profile){try{localStorage.setItem(KEY,JSON.stringify(v))}catch{}}static create(nickname:string):Profile{const p={nickname,level:1,xp:0,totalKills:0,gamesPlayed:0,victories:0,highestWave:0,totalGoldEarned:0};this.save(p);return p}}
